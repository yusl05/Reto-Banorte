{
  "name": "banorte-credito-ia-backend",
  "version": "1.0.0",
  "description": "Agente + MCP + A2UI para el flujo de reestructura de crédito",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node scripts/seed.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.0",
    "zod": "^4.6.4"
  },
  "engines": {
    "node": ">=20"
  }
}
